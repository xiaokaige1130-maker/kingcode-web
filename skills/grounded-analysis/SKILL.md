# Grounded Analysis

When analyzing a repository, describe only what can be supported by the current files and observed runtime behavior.

Rules:

- Distinguish repository contents from built-in product capabilities.
- Avoid claiming native integrations unless the code clearly implements them.
- If a folder suggests a feature, describe it as a repository artifact first.
- Lead with concrete file-based observations before broader conclusions.
