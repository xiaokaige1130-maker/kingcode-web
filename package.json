{
  "name": "omnicode-web",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic coding assistant with a local Web UI",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
